# Priority environment file for PR automation ONLY.
# Copy to .env.pr (kept out of VCS) and set your token.
# This file is ignored; do NOT put the real token here.
GITHUB_TOKEN=REPLACE_WITH_YOUR_GITHUB_TOKEN
